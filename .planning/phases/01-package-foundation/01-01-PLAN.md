---
phase: 01-package-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - PKG-01
  - PKG-02

must_haves:
  truths:
    - "npm tarball contains only simulator.mjs, example-config.json, README.md, and LICENSE"
    - "npm registry page shows author, homepage, and bugs links"
    - "No test files, .planning directory, or dotfiles appear in tarball"
  artifacts:
    - path: "package.json"
      provides: "files whitelist and registry metadata"
      contains: "files"
  key_links:
    - from: "package.json files field"
      to: "npm pack output"
      via: "npm tarball inclusion logic"
      pattern: "\"files\""
---

<objective>
Add `files` whitelist and professional metadata fields to package.json so the npm tarball contains only intended files and the registry page looks complete.

Purpose: PKG-01 and PKG-02 — ensure the published package is lean and the npm page has author/homepage/bugs links.
Output: Updated package.json with `files`, `author`, `homepage`, `bugs` fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add files whitelist and metadata fields to package.json</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
Edit package.json to add the following fields:

1. **`files` array** (per PKG-01) — Add immediately after the `"main"` field:
```json
"files": [
  "simulator.mjs",
  "example-config.json",
  "README.md",
  "LICENSE"
],
```

2. **`author` field** (per PKG-02) — Add after `"license"`:
```json
"author": "Panoptris (https://panoptris.com)",
```

3. **`homepage` field** (per PKG-02) — Add after `"author"`:
```json
"homepage": "https://github.com/YuYongJu/dji-cloud-simulator#readme",
```

4. **`bugs` field** (per PKG-02) — Add after `"homepage"`:
```json
"bugs": {
  "url": "https://github.com/YuYongJu/dji-cloud-simulator/issues"
},
```

Note: `repository` already exists. Do NOT duplicate it. The `author` uses "Panoptris" because the LICENSE copyright belongs to Panoptris (per project constraint in CLAUDE.md: "Panoptris copyright in LICENSE stays as-is").
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const ok = Array.isArray(p.files) && p.files.length===4 && p.author && p.homepage && p.bugs && p.bugs.url; if(!ok){process.exit(1)} console.log('OK: all fields present')"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c '"files"' package.json` returns 1
- `grep -c '"author"' package.json` returns 1
- `grep -c '"homepage"' package.json` returns 1
- `grep -c '"bugs"' package.json` returns 1
- `node -e "const p=require('./package.json'); console.log(p.files)"` prints array with exactly simulator.mjs, example-config.json, README.md, LICENSE
  </acceptance_criteria>
  <done>package.json contains files whitelist with exactly 4 entries and author/homepage/bugs metadata fields</done>
</task>

<task type="auto">
  <name>Task 2: Verify npm pack produces clean tarball</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
Run `npm pack --dry-run` and verify the output contains ONLY the intended files. The expected files in the tarball are:
- package.json (always included by npm)
- simulator.mjs
- example-config.json
- README.md
- LICENSE

If any unintended files appear (test files, .planning/, dotfiles, package-lock.json), fix the `files` field in package.json.

This is a verification-only task. If Task 1 was correct, no edits are needed.
  </action>
  <verify>
    <automated>npm pack --dry-run 2>&1 | grep -E "^npm notice" | grep -v "==" | grep -v "name:" | grep -v "version:" | grep -v "filename:" | grep -v "package size:" | grep -v "unpacked size:" | grep -v "shasum:" | grep -v "integrity:" | grep -v "total files:" | grep -v "bundled deps:" | wc -l | xargs test 5 -eq</automated>
  </verify>
  <acceptance_criteria>
- `npm pack --dry-run 2>&1` output lists exactly 5 files: package.json, simulator.mjs, example-config.json, README.md, LICENSE
- No .planning/ files appear in dry-run output
- No .git files appear in dry-run output
- No package-lock.json appears in dry-run output
  </acceptance_criteria>
  <done>npm pack --dry-run confirms tarball contains only the 5 intended files (package.json + 4 whitelisted)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json -> npm registry | Metadata fields become public on npmjs.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | package.json files field | mitigate | files whitelist ensures only intended source ships; npm pack --dry-run verifies before publish |
| T-01-02 | Tampering | package.json metadata | accept | Low risk — author/homepage/bugs are public marketing fields, not security-sensitive |
</threat_model>

<verification>
1. `node -e "const p=require('./package.json'); console.assert(p.files.length===4); console.assert(p.author); console.assert(p.homepage); console.assert(p.bugs.url); console.log('PASS')"` prints PASS
2. `npm pack --dry-run 2>&1 | grep "total files"` shows 5
3. `npm pack --dry-run 2>&1` contains no .planning, .git, or test references
</verification>

<success_criteria>
- package.json has `files` array with exactly: simulator.mjs, example-config.json, README.md, LICENSE
- package.json has `author`, `homepage`, `bugs` fields populated with correct values
- `npm pack --dry-run` output contains exactly 5 files (the 4 whitelisted + package.json)
- No unintended files leak into the tarball
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-foundation/01-01-SUMMARY.md`
</output>
